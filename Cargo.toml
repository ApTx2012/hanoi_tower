[package]
name = "gear_tower_gui"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.22.0"
# 手动开启eframe需要的winapi特性，消除winuser/windef报错
winapi = { version = "0.3.9", features = ["winuser", "windef", "handleapi", "winbase"] }